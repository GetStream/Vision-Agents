name: flower_spotter
description: A voice agent that looks through the caller's camera.
llm: llm-fast
subagents:
  default: llm-thinking
  vision: vlm
video:
  source: roboflow_streaming
  max_frames: 1
