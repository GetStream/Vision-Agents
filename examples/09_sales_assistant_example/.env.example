# Stream API credentials (required)
# Get these from https://dashboard.getstream.io
# The API key must match the one used by the Flutter app (see overlay_app.dart).
STREAM_API_KEY=your_stream_api_key_here
STREAM_API_SECRET=your_stream_api_secret_here

# Google Gemini API key (required)
# Get a free key from https://aistudio.google.com
GOOGLE_API_KEY=your_google_api_key_here

# Deepgram API key (required for speech-to-text)
# Get a free key from https://console.deepgram.com
DEEPGRAM_API_KEY=your_deepgram_api_key_here

# Optional: base URL for demo join links printed by the agent server
# EXAMPLE_BASE_URL=https://getstream.io/video/demos
