[project]
name = "huggingface-transformers-example"
version = "0.0.0"
requires-python = ">=3.10"

dependencies = [
  "python-dotenv>=1.0",
  "vision-agents-plugins-huggingface[transformers]",
  "vision-agents",
]

[tool.uv.sources]
"vision-agents-plugins-huggingface" = {path = "../..", editable=true}
"vision-agents" = {path = "../../../../agents-core", editable=true}
