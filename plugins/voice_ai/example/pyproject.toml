[project]
name = "voice-ai-tts-example"
version = "0.0.0"
requires-python = ">=3.10"

dependencies = [
  "python-dotenv>=1.0",
  "vision-agents-plugins-voice-ai",
  "vision-agents",
]

[tool.uv.sources]
"vision-agents-plugins-voice-ai" = {path = "..", editable = true}
"vision-agents" = {path = "../../../agents-core", editable = true}
