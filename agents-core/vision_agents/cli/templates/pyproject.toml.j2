[project]
name = "{{project_name}}"
version = "0.0.0"
requires-python = ">=3.10"

dependencies = [
  "python-dotenv>=1.0",
  "vision-agents",
  "vision-agents-plugins-openai",
  "vision-agents-plugins-getstream",
]
